global
	log 127.0.0.1	local0
  	log-send-hostname
	chroot /var/lib/haproxy
	stats socket /run/haproxy/admin.sock mode 660 level admin expose-fd listeners
	stats timeout 30s
	user haproxy
	group haproxy
	daemon

	# Default SSL material locations
	ca-base /etc/ssl/certs
	crt-base /etc/haproxy/ssl
	#crt-base /etc/ssl/private

  	tune.bufsize 65536
	tune.ssl.default-dh-param 2048
	# Default ciphers to use on SSL-enabled listening sockets.
	# For more information, see ciphers(1SSL). This list is from:
	#  https://hynek.me/articles/hardening-your-web-servers-ssl-ciphers/
	# An alternative list with additional directives can be obtained from
	#  https://mozilla.github.io/server-side-tls/ssl-config-generator/?server=haproxy
	# ssl-default-bind-ciphers ECDH+AESGCM:DH+AESGCM:ECDH+AES256:DH+AES256:ECDH+AES128:DH+AES:RSA+AESGCM:RSA+AES:!aNULL:!MD5:!DSS
  	ssl-default-bind-ciphers AES:ALL:!NULL:!aNULL:!RC4:!RC2:!MEDIUM:!LOW:!EXPORT:!DES:!MD5:!PSK:!3DES:@STRENGTH
	ssl-default-bind-options no-sslv3

defaults
	log		  global
	mode	  http
	balance roundrobin
	option	httplog
	option forwardfor except 127.0.0.1
  	#option  log-separate-errors
	#option	dontlognull
	option  http-server-close
	option  tcp-smart-accept
	option  tcp-smart-connect
	option  accept-invalid-http-response
	option  splice-auto
  	retries 3
  	timeout connect 50000
  	timeout client  90000
  	timeout server  90000
	timeout queue   10s
	timeout http-request 15s
	timeout http-keep-alive 5s
	errorfile 400 /etc/haproxy/errors/400.http
	errorfile 403 /etc/haproxy/errors/403.http
	errorfile 408 /etc/haproxy/errors/408.http
	errorfile 500 /etc/haproxy/errors/500.http
	errorfile 502 /etc/haproxy/errors/502.http
	errorfile 503 /etc/haproxy/errors/503.http
	errorfile 504 /etc/haproxy/errors/504.http


frontend mobile-in
	log	global
	bind *:9080
	option forwardfor

	capture request header Host len 128
	capture request header X-Forwarded-For len 128
	capture request header User-Agent len 128
	capture request header Referer len 128

	# PHP7 ACL
	# acl use_php7 urlp_sub(codven) 00002

	# Blocked IPs ACL
	acl is-blocked-ip src -f /etc/haproxy/blocked_ips
	http-request deny if is-blocked-ip

	# Block non xampp URIs
	acl xampp-uri path_beg	/xampp
	http-request deny unless xampp-uri

	# Backend Choice
	#use_backend lamp7	if use_php7
	#default_backend xampp5
	default_backend lamp7


backend lamp7
	option httpchk GET /xampp/healthcheck
	option forwardfor
	#http-request add-header X-Forwarded-For %[src]
	http-request add-header X-Real-IP %[src]
	server	websrv01 127.0.0.1:80	slowstart 15s weight 100 check inter 3000 rise 3 fall 5

#backend xampp5
#	option httpchk GET /xampp/healthcheck
#	option forwardfor
#	server	wservidor02 192.168.1.200:8080	slowstart 15s weight 100 check inter 30000 rise 3 fall 5
#	#server	websrv01 127.0.0.1:81	slowstart 15s weight 100 check inter 30000 rise 3 fall 5

